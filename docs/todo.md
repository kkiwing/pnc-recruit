# TODO

마지막 갱신: 2026-07-18 (발송 메시지 설정 개선 + 개별 발송 도입 시점 기준)

## 완료한 작업

- [x] 대시보드 — 공고 현황 요약(진행중 공고 기준), 공고별 현황 테이블
- [x] 공고 관리 — CRUD, 검색/필터/정렬, 공개 여부 토글, 3구역 그리드 카드(지표를 아이콘+라벨+숫자로 표시), 지원자·별도관리 수 바로가기
- [x] 공고 상세 — 허브 화면(기본 정보/본문/자소서 문항/프로세스 요약 + 바로가기 카드 3종)
- [x] 프로세스 관리 — 공고별/프리셋별 전형 단계 추가·삭제·드래그 순서 변경, 단계별 상태값 커스터마이징(이름/색상/날짜입력), 상태값·단계 삭제 시 지원자 자동 이동, 자동 발송 문구 설정(기본 템플릿 포함, 실제 발송은 미구현)
- [x] 지원자 목록 — 목록형/파이프라인(칸반) 토글 뷰, 검색/필터(팀/단계/상태)/정렬 툴바, 최종 결과 지정 시 전형 조작 UI 잠금
- [x] 지원자 상세 — 기본 인적사항, 학력/자격증/경력/활동/통계 패키지/논문, 자기소개서, 메모, 첨부파일, 진행 스테퍼 + 최종 결과 배지
- [x] 별도 관리 — 지원자 목록과 통합된 테이블(공유 컴포넌트), 사유 자유 텍스트 입력/수정, 목록 복귀
- [x] 면접 일정 — 캘린더 + 타임테이블, 예정/지난/완료 3분류
- [x] **전형 구조를 "세분화된 7단계"에서 "국면(4단계) + 상태"로 재편**(2026-07-15) — 완료입력폼을 단계 속성에서 상태 속성(`hasDateInput`)으로 이전
- [x] **단계별 합불 판정 개념 완전 폐지**(2026-07-15/16) — `isPass`/`isFail`/`stageType` 제거, 최종 합불은 전형 구조와 분리된 `Applicant.finalResult`로 일원화. 기본 프리셋에서도 합격/불합격 이름의 상태값 자체를 제거
- [x] **최종 결과 지정 시 전형 조작 UI 잠금**(2026-07-15) — 목록/파이프라인/상세 3화면 공통 적용, 판정 해제 시 자동 복구
- [x] **별도관리 사유를 고정 목록에서 자유 텍스트로 전환**(2026-07-15)
- [x] **상태값/단계 삭제 시 지원자 자동 이동 규칙**(2026-07-17) — 영향받는 인원수·이동처를 확인 모달에 안내 후 즉시 커밋, 프리셋 편집 중에는 이동 로직 미적용
- [x] **상태 플래그(시작/단계종료)를 완전 위치 기반 자동화**(2026-07-17) — "단계종료" 체크박스 제거, `isDefault`(맨 위)와 대칭으로 `isCompletion`(맨 아래) 자동 계산
- [x] **상태값/단계 순서 변경을 실제 드래그앤드롭으로 전환**(2026-07-17) — 도트 핸들, 반투명+삽입선 피드백, 핸들 판정 버그(HTML5 dragstart의 target 특성) 발견·수정
- [x] **발송 메시지 설정 개선 + 개별 발송(프로토타입 수준)**(2026-07-18) — 템플릿 폼 상시 노출·토글은 "자동 발송 사용"만 담당, 지원자 날짜 입력 모달에 변수 치환 미리보기+발송 버튼, 발송 시 `meta.send` 기록만 생성(재발송 시 갱신), 자동 발송 켜진 단계는 상태 변경 확정 시 자동 기록, 상세 스테퍼에 발송 아이콘 (functional-spec 2.15)
- [x] **기본 프로세스 프리셋 관리 기능**(2026-07-14) — 공고와 별개로 편집 가능한 전역 템플릿, 신규 공고 생성 시에만 스냅샷 복사
- [x] 공고 상세를 허브로 재편, 화면 간 `?posting=` 쿼리 연결(2026-07-14)
- [x] 디자인 시스템 적용 — 색상/그림자/라운딩/폰트 토큰 정리, 전 화면에 하드코딩 색상·하드 보더 제거 반영(2026-07-13)

## 진행 중 작업

현재 진행 중인 작업은 없습니다. 마지막 작업(발송 메시지 설정 개선 + 개별 발송)까지 커밋 및 반영 완료된 상태입니다.

## 남은 작업

아래 항목은 이 프로토타입 단계에서 다루지 않은, 실제 서비스 구축 시 필요한 과제입니다(전부 여전히 미완료).

- [ ] 매트릭스(단계별 컬럼) 지원자 테이블 재도입 여부 결정 — 컴포넌트(`ApplicantTable.tsx`)는 미사용 상태로 보존 중
- [ ] 실제 알림/메일·문자 발송 연동 (템플릿·변수 치환 미리보기·개별 발송 UX·발송 기록까지는 있음, 실제 메일/SMS를 내보내는 로직은 없음. 발송 이력 테이블·회사명 설정화면도 연동 시 필요 — decision-log 2026-07-18)
- [ ] 첨부파일 영속 저장 (현재는 브라우저 세션 내 임시 blob URL)
- [ ] 인증/권한 체계 설계 (현재 프로토타입에는 로그인/권한 구분 없음)
- [ ] 오퍼/입사 온보딩 프로세스 (처우 협의, 입사일 확정 등)
- [ ] 전형 단계 변경 이력(감사 로그) — 누가 언제 무엇을 변경했는지 추적 불가
- [ ] 면접 평가표(스코어카드) — 현재 면접 단계는 진행 여부(안내/진행완료)만 기록하고, 면접관별 평가 항목·점수·의견 같은 정성적 기록 기능은 없음(최종 합불은 면접 단계와 무관하게 `finalResult`로 판단)
- [ ] 테스트 커버리지 확충 (현재 예시 테스트 1개 수준)
- [ ] 실제 백엔드/DB 연동 설계 (데이터 모델은 `src/types/` 참고)
- [ ] 지원자 데이터 유입 방식(어드민 사전 등록 + 매칭 가입) 검토 — 아직 방향만 검토 중, 결정된 사항 아님(`decision-log.md` 2026-07-14 참고)

## 다음 작업 순서 및 우선순위

1. **P0 — 테크팀 리뷰**: 현재 화면 흐름과 데이터 모델(`docs/functional-spec.md`, `src/types/`)을 테크팀과 함께 리뷰하고 실제 구축 범위를 합의
2. **P1 — 인증/권한 요구사항 정의**: 담당자별 권한 구분이 필요한 범위 확정
3. **P1 — 알림/메일 발송 요구사항 정의**: 발송 메시지 설정·개별 발송 UX·발송 기록까지 프로토타입에 있으므로, 실제 발송 연동 방식(사내 메일 시스템 연동 등)만 정하면 됨 — 현재의 발송 기록 지점이 그대로 실제 발송 호출 지점이 됨
4. **P2 — 파일 스토리지 연동 방식 결정**: 첨부파일을 어디에 영속 저장할지
5. **P2 — 변경 이력(감사 로그) 요구사항 정의**
6. **P3 — 오퍼/온보딩 프로세스 설계**
7. **P3 — 매트릭스 뷰 재도입 여부 결정**
8. **P3 — 지원자 데이터 유입 방식(사전 등록 + 매칭) 설계**: 2차 범위인 "외부 지원 기능"과 함께 다뤄야 함

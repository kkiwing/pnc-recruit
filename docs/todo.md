# TODO

마지막 갱신: 2026-07-14 (공고 상세 허브 재편 완료 시점 기준)

## 완료한 작업

- [x] 대시보드 — 공고 현황 요약, 공고별 현황 테이블
- [x] 공고 관리 — CRUD, 검색/필터/정렬, 공개 여부 토글, 지원자·별도관리 수 바로가기
- [x] 공고 상세 — 허브 화면(기본 정보/본문/자소서 문항/프로세스 요약 + 바로가기 카드 3종)
- [x] 프로세스 관리 — 공고별 전형 단계 추가/삭제/순서 변경, 단계 유형(일반/합불 판정), 완료 입력폼, 상태값 커스터마이징(이름/색상/기본/완료/합격/불합격 플래그), 자동 발송 문구 설정 UI
- [x] 지원자 목록 — 목록형/파이프라인(칸반) 토글 뷰, 검색/필터(팀/단계/상태)/정렬 툴바
- [x] 지원자 상세 — 기본 인적사항, 학력/자격증/경력/활동/통계 패키지/논문, 자기소개서, 메모, 첨부파일
- [x] 별도 관리 — 지원자 목록과 통합된 테이블(공유 컴포넌트), 사유 필터, 목록 복귀
- [x] 면접 일정 — 캘린더 + 타임테이블, 예정/완료 목록
- [x] 전형 단계/상태 오판정 버그 수정 — 순서/이름 문자열 의존 → 명시적 플래그(`stageType`/`isPass`/`isFail`/`isCompletion`) 기반으로 전환
- [x] 디자인 시스템 적용 — 색상/그림자/라운딩/폰트 토큰 정리, 전 화면에 하드코딩 색상·하드 보더 제거 반영
- [x] 화면 간 URL 연결 — 공고 상세 ↔ 지원자 목록/별도 관리/프로세스 관리를 `?posting=` 쿼리로 연결

## 진행 중 작업

현재 진행 중인 작업은 없습니다. 마지막 작업(공고 상세 허브 재편)까지 커밋 및 반영 완료된 상태입니다.

## 남은 작업

- [ ] 매트릭스(단계별 컬럼) 지원자 테이블 재도입 여부 결정 — 컴포넌트(`ApplicantTable.tsx`)는 미사용 상태로 보존 중
- [ ] 실제 알림/메일·문자 발송 연동 (현재는 발송 문구를 설정하는 화면만 존재, 실제 발송 없음)
- [ ] 첨부파일 영속 저장 (현재는 브라우저 세션 내 임시 blob URL)
- [ ] 인증/권한 체계 설계 (현재 프로토타입에는 로그인/권한 구분 없음)
- [ ] 오퍼/입사 온보딩 프로세스 (처우 협의, 입사일 확정 등)
- [ ] 전형 단계 변경 이력(감사 로그) — 누가 언제 무엇을 변경했는지 추적 불가
- [ ] 면접 평가표(스코어카드) — 현재는 합격/불합격 이진값만 기록
- [ ] 테스트 커버리지 확충 (현재 예시 테스트 1개 수준)
- [ ] 실제 백엔드/DB 연동 설계 (데이터 모델은 `src/types/` 참고)

## 다음 작업 순서 및 우선순위

1. **P0 — 테크팀 리뷰**: 현재 화면 흐름과 데이터 모델(`docs/project-summary.md`, `src/types/`)을 테크팀과 함께 리뷰하고 실제 구축 범위를 합의
2. **P1 — 인증/권한 요구사항 정의**: 담당자별 권한 구분이 필요한 범위 확정
3. **P1 — 알림/메일 발송 요구사항 정의**: 자동 발송 설정 UI는 이미 있으므로, 실제 발송 연동 방식(사내 메일 시스템 연동 등)만 정하면 됨
4. **P2 — 파일 스토리지 연동 방식 결정**: 첨부파일을 어디에 영속 저장할지
5. **P2 — 변경 이력(감사 로그) 요구사항 정의**
6. **P3 — 오퍼/온보딩 프로세스 설계**
7. **P3 — 매트릭스 뷰 재도입 여부 결정**
